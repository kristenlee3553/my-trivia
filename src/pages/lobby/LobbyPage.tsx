import { Box } from "@mui/material";
import ThemeWrapper from "../../common/theme";

export default function LobbyPage() {
  return (
    <ThemeWrapper>
      <Box>Lobby</Box>
    </ThemeWrapper>
  );
}
